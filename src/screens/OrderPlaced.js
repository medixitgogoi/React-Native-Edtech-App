import { View, Text } from 'react-native';

const OrderPlaced = () => {
    return (
        <View style={{ flex: 1 }}>
            <Text style={{ color: '#000' }}>OrderPlaced</Text>
        </View>
    )
}

export default OrderPlaced;